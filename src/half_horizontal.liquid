<div class="screen">
  <div class="view view--half_horizontal">

    <div class="title_bar">
      <p class="title">{{ countdown_title }}</p>
    </div>

    <div class="layout layout--row p--4 gap--[16px]">
      {% if countdown_image != blank %}
        <img class="image image--contain image-dither countdown-image countdown-image--row-end"
          src="data:image/png;base64,{{ countdown_image }}"
          alt="" />
      {% endif %}
      <p class="text--base">{{ countdown_label }}</p>
    </div>

  </div>
</div>
